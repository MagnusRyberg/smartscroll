{
  "name": "smartscroll-static-app",
  "version": "1.0.0",
  "description": "Wikipedia article scroller deployed as Azure Static Web App",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.2"
  }
}